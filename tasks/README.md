# Tasks

This directory holds active scoped work that should not be overloaded into the repo-level `PLANS.md`.

- `active/`
  active implementation tasks and investigations

Use `PLANS.md` for the main delivery path and `tasks/active/` for bounded side work.
